import React from 'react';
import ReactDOM from 'react-dom';

const Feedback = () => {
  const getFeedback = (e: React.MouseEvent<HTMLButtonElement>) => {
    //this cuold be an event started from the submit button component, or we coud get rid of that and put it here
  };

  const displayFeedback = () => {};
  return 'x';
};

export default Feedback;
